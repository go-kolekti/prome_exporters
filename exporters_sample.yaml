inputs:
  - name: prometheus_node_exporter
  - name: prometheus_go
    interval: 15s
  - name: http_metrics
    options:
      host: 127.0.0.1
      port: 80
      metrics_path: /metrics

output:
  name: http

tags:
  instance: 127.0.0.1
