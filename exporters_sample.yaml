inputs:
  - name: prometheus_node_exporter
  - name: http_metrics
    options:
      host: 127.0.0.1
      port: 80
      metrics_path: /metrics

output:
  name: http

tags:
  key: value

command_type : 0
